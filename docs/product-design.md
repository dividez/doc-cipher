# DocCipher 产品设计 v0.3

## 产品定位

DocCipher 是一个完全离线的 Word `docx` 脱敏与还原桌面客户端。

它不是 Word 编辑器，也不是多人协作文档工具。产品核心是帮助用户在本地完成：

```txt
选择 docx -> 预览并确认敏感内容 -> 执行脱敏 -> 生成 masked docx + restore.enc
masked docx + restore.enc -> 本地部分还原 + restore-report.json
```

核心价值：

- 原文不出本机
- 脱敏可重复、可还原
- 不依赖云端服务
- 支持按业务场景维护脱敏方案
- 支持用户划词补充方案关键词

能力边界详见 [capability-guide.md](capability-guide.md)。

## 目标用户

主要用户是需要处理合同、制度、标书、审计材料、交付材料的业务或运营人员。

典型用户不一定理解正则或 OOXML，但能判断文档中哪些词需要脱敏。因此 UI 应优先支持：

- 选择文件
- 阅读预览
- 划词标注
- 选择脱敏方案
- 执行脱敏
- 找到输出文件

高级正则、JSON 配置、日志等能力应保留，但不作为主要路径。

## 核心概念

### 默认规则模板

默认规则模板是新建脱敏方案时的基础规则来源，存放在设置中。

它适合存放通用规则，例如：

- 手机号、身份证号、邮箱、银行账号（正则）
- 通用关键词（规则 id：`keywords`，即**系统关键词**）

修改默认规则模板只影响未来新建方案，不应悄悄覆盖已经保存的方案。

### 脱敏方案

脱敏方案是面向业务场景的一组规则配置（`MaskProfile`）。

一个方案包含完整 `settings`，其中方案专属关键词使用规则 id：`profile_keywords`（**方案关键词**）。

### 应用级开关

在设置页可全局控制：

- **正则规则**：是否应用模板中的 regex 规则
- **系统关键词**：是否应用模板中 `keywords` 规则
- **默认输出目录**

脱敏与命中预估均尊重上述开关。

### 本次手动标注

只属于当前打开的 docx，使用 part、段落序号与 offset 精确定位。可跨段落划词（`segments`）。

保存方案时，划词文本合并进方案关键词，并清空本次手动列表。

## 信息架构（当前实现）

```txt
工作台
├── 选择 docx / 拖入
├── 最近任务
└── 任务历史（本地索引）

脱敏
├── 左侧：文档预览
│   ├── 划词、右键菜单、手动高亮
│   ├── 规则命中高亮（切换方案 / 开文档后自动刷新）
│   └── Ctrl/Cmd+F 搜索
├── 右侧：方案、手动标注、规则摘要、密码、执行
│   └── 命中详情：弹窗（不占右侧栏）
├── 命中预估按钮 +「已预估 N 处」链接
└── 方案管理入口

还原
├── masked docx + restore.enc + 密码
├── 精确 token 部分还原
├── 输出 restored.docx + restore-report.json
└── 成功后清空表单（保留输出目录）

方案管理
├── 导入 / 导出 / 复制
└── 方案关键词编辑（与系统关键词区分说明）

设置
├── 快捷开关（正则、系统关键词、默认输出目录）
├── 规则列表
└── 高级 JSON
```

## 脱敏主流程

敏感信息四类来源：**正则**、**方案/系统关键词**、**划词备选词**、**AI 辅助识别**；统一在脱敏前确认，最后点「开始脱敏」。

1. 选择 docx，左侧 **文档预览** 立即加载正文；选择脱敏方案，划词加入备选列表。
2. **扫描文档**：规则与关键词命中统计（`docx:recognize-matches`，不改写文件）。
3. 可选 **AI 辅助识别**：脱敏页按钮触发，显示进度；结果写入备选列表（模型仅在设置页选用）。
4. **开始脱敏**：填写还原密码后执行；`docx:mask` 应用 `recognizedHits` 快照。
5. 脱敏成功后预览切换为 masked.docx。

## 还原主流程

1. 用户选择当前待还原 docx、`restore.enc`，并输入还原密码。
2. Main 解密 `restore.enc`。密码错误或映射结构非法时直接失败。
3. Main 计算当前 docx SHA-256。若与映射中的 `masked_doc_fingerprint` 不一致，说明文档可能被用户编辑；继续执行部分还原，并在日志和报告中标记。
4. Main 遍历 OOXML 文本 part，按段落拼接 `<w:t>` 内容，只替换完整且精确命中 token vault 的 token。
5. 被删除、改坏或不完整的 token 跳过；不做模糊猜测。
6. 同一合法 token 被复制多次时，所有出现位置都还原为同一个原文。
7. 输出 `*.restored.docx`、`restore-report.json`、manifest、日志。

`restore-report.json` 展示：

- 原始 token 总数
- 成功还原 token 数
- 实际替换次数
- 未找到 token 数
- 未知 token 数
- 每个 token 的 `restored` / `missing` / `unknown` 状态

报告不写入原文，避免形成新的敏感数据文件。

## Renderer / Main 分工

| 能力         | Renderer                    | Main                                         |
| ------------ | --------------------------- | -------------------------------------------- |
| 版式预览     | docx-preview 渲染           | `docx:read-file` 读二进制                    |
| 划词         | `getSelection().toString()` | —                                            |
| 脱敏 / 还原  | 展示结果与报告摘要          | OOXML 替换、写 masked / restore.enc / report |
| 识别结果     | 「识别结果」对话框          | `docx:recognize-matches`                     |
| 文档预览高亮 | 规则/划词 + 识别命中        | Renderer `docx-preview-highlights`           |

原则：文档预览不写 docx；脱敏不依赖预览 DOM；识别与脱敏分离。

## 文档预览 vs 识别结果

| 组件               | 职责                                | 时机             |
| ------------------ | ----------------------------------- | ---------------- |
| 文档预览（左栏）   | 已选 docx 正文、划词、规则/识别高亮 | 选文件后即加载   |
| 识别结果（对话框） | 命中分类统计与样例                  | 扫描或 AI 识别后 |

- IPC：`docx:recognize-matches`（扫描 / `aiSupplementOnly` 补充），不修改源文件。
- 备选词列表统一收纳划词与 AI 词，可删除；脱敏时按列表过滤 AI/划词命中。
- AI 识别进行中可 `ai:cancel-mask` 取消。

## 关键词：系统 vs 方案

| 类型       | 规则 ID            | 配置            | 开关                     |
| ---------- | ------------------ | --------------- | ------------------------ |
| 系统关键词 | `keywords`         | 设置 → 默认模板 | `enable_system_keywords` |
| 方案关键词 | `profile_keywords` | 当前脱敏方案    | 随方案启用               |

脱敏页规则摘要、命中弹窗、方案编辑页均区分两类关键词。

## 划词交互

- 左键划词 →「加入脱敏」→ 高亮 → 进入手动列表
- 右键：加入脱敏、复制、清空当前选择
- 工具栏「清空标注」：清空当前文档全部手动项

## 视觉与布局

```txt
左侧：文档预览（独立滚动）
右侧：方案、手动标注、规则 chips、密码、执行（保持紧凑）
弹窗：命中详情（最大高度约 70vh）
```

## 非目标（当前）

- Word 编辑器、多人协作、云端存储
- OCR、LLM 语义审核
- PDF / 非 docx 格式

## 后续演进

已完成（v0.3）：跨段划词、方案导入导出/复制、任务历史、命中预估与弹窗、应用开关、预览搜索、结构标签优化、系统/方案关键词区分。

建议后续：

1. PDF 支持或导出链路
2. 规则命中与脱敏质量反馈（误报/漏报标记）
3. 表格单元格级预览与定位增强
